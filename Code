// Fixed content for the Code file
// 1) Initial draw() call before the game starts
function startGame() {
    draw();
    // Other initialization code
}

// 2) Proper dino state reset on game restart
function resetDinoState() {
    dino.position = { x: initialX, y: initialY };
    dino.velocity = 0;
    dino.grounded = true;
    // other state reset code
}